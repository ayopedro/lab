# .env.sample - Copy to .env and fill in secure values.
# Never commit your real .env with secrets.

# Postgres
POSTGRES_USER=ayotunde
POSTGRES_PASSWORD=change_me
POSTGRES_DB=app_dev

# MySQL / MariaDB
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_DATABASE=app_dev
MYSQL_USER=app_user
MYSQL_PASSWORD=change_me_user

# Redis
REDIS_PASSWORD=change_me
# REDIS_URL=redis://localhost:6379

# pgAdmin (optional - uncomment pgadmin service in docker-compose.yml to use)
# PGADMIN_EMAIL=admin@lab.local
# PGADMIN_PASSWORD=change_me_admin

# GPG / Signing (optional placeholders)
GPG_KEY_ID=<YOUR_GPG_KEY_ID>

# Node / NVM defaults (optional)
NODE_VERSION=lts

# Add any additional service credentials below
# EXAMPLE_API_KEY=xxx
