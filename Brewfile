# Brewfile - Core macOS developer tooling

brew "git"
brew "wget"
brew "curl"
brew "zsh"
brew "gnupg"
brew "jq"
brew "go"
brew "node"

cask "docker"
cask "visual-studio-code"
cask "hyper"
cask "dbeaver-community"

# Optional (comment or remove if not needed)
# brew "postgresql"  # Using docker container instead
# brew "mysql"       # Using docker container instead
# brew "mas"         # Mac App Store CLI
# brew "redis"       # Using docker-compose service instead; uncomment if you want local daemon
